{
  "name": "regres",
  "version": "1.0.0",
  "description": "",
  "main": "delete-user.test.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "npx mocha tests --reporter mochawesome --reporter-options reportDir=customReportDir,reportFilename=api-results --timeout 10000",
    "curs9": "npx mocha curs9 --reporter mochawesome --reporter-options reportDir=customReportDir,reportFilename=api-results --timeout 10000"
  },
  "author": "Radu Gafton",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.0.1",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "npm": "^9.6.7",
    "npx": "^10.2.2",
    "pactum": "^3.4.0"
  }
}
